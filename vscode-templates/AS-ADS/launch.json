{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "AS-ADS: Debug (OpenOCD + ST-Link)",
            "type": "cortex-debug",
            "request": "launch",
            "servertype": "openocd",
            "cwd": "${workspaceFolder}",
            "executable": "${workspaceFolder}/build/ADS.elf",
            "configFiles": ["interface/stlink.cfg", "target/stm32f4x.cfg"],
            "device": "STM32F407VG",
            "runToEntryPoint": "main",
            "preLaunchTask": "AS-ADS: Build"
        },
        {
            "name": "AS-ADS: Attach (OpenOCD + ST-Link)",
            "type": "cortex-debug",
            "request": "attach",
            "servertype": "openocd",
            "cwd": "${workspaceFolder}",
            "executable": "${workspaceFolder}/build/ADS.elf",
            "configFiles": ["interface/stlink.cfg", "target/stm32f4x.cfg"],
            "device": "STM32F407VG"
        }
    ]
}
